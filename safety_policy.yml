policy:
  fail_on:
    severity:
      - high
    confidence:
      - high
      - medium

  license:
    blocklist:
      - "Proprietary"

  exemptions:
    - package: Jinja2
      reason: "This package is used in a context where the vulnerability does not pose a security risk for the user."
      id: 70612

security:
  ignore_unpinned_requirements: false
